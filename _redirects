# Caddygate redirects
# 2026-05-31: 旧 UI 全ページ復活のため、トップへの全リダイレクトを解除
# Cloudflare Pages はファイル拡張子なしの clean URL を自動解決するため、
# /login → /login/index.html, /dashboard → /dashboard.html は無設定で動作する。
# 個別リダイレクトが必要になったらここに追記する。
